# config/samples/postgres-test.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: kflashback-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_DB
              value: kflashback
            - name: POSTGRES_USER
              value: kflashback
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - kflashback
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: kflashback-system
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432